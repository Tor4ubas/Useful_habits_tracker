[tool.poetry]
name = "useful-habits-tracker"
version = "0.1.0"
description = ""
authors = ["Tor4ubas <alex1029@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.0.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
